import { LojchartDirective } from './lojchart.directive';

describe('LojchartDirective', () => {
  it('should create an instance', () => {
    const directive = new LojchartDirective();
    expect(directive).toBeTruthy();
  });
});
